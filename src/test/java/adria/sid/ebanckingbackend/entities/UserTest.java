package adria.sid.ebanckingbackend.entities;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

class UserTest {

    /*Test
    void testGettersAndSetters() {
        // Test the getters and setters
        User user = new User();
        user.setId(1L);
        user.setNom("John");
        user.setPrenom("Doe");
        user.setUserName("johndoe");
        user.setPassword("mypassword");

        assertEquals(1L, user.getId());
        assertEquals("John", user.getFirstName());
        assertEquals("Doe", user.getLastName());
        assertEquals("johndoe", user.getUserName());
        assertEquals("mypassword", user.getPassword());
    }

    @Test
    void testNotificationsAndComptes() {
        // Test adding notifications and comptes
        User user = new User();
        Notification notification1 = new Notification("Notification 1");
        Notification notification2 = new Notification("Notification 2");
        Compte compte1 = new Compte("Account 1");
        Compte compte2 = new Compte("Account 2");

        user.addNotification(notification1);
        user.addNotification(notification2);
        user.addCompte(compte1);
        user.addCompte(compte2);

        assertEquals(2, user.getNotifications().size());
        assertTrue(user.getNotifications().contains(notification1));
        assertTrue(user.getNotifications().contains(notification2));

        assertEquals(2, user.getComptes().size());
        assertTrue(user.getComptes().contains(compte1));
        assertTrue(user.getComptes().contains(compte2));
    }

    @Test
    void testRole() {
        // Test the role association
        User user = new User();
        Role role = new Role("ROLE_ADMIN");

        user.setRole(role);

        assertEquals(role, user.getRole());
    }*/
}
